# Git Workflow - Как работать с кодом профессионально

## 🎯 Проблема

Ты залил код на GitHub в `main` ветку. Теперь все видят твой код.
Как писать новый код, не ломая продакшн?

## ✅ Решение: Ветки (Branches)

### Концепция:

```
main (продакшн - все видят)
  ↓
develop (разработка - только ты)
  ↓
feature-alerts (новая фича - только ты)
```

---

## 📝 Workflow для новой фичи

### Шаг 1: Создай ветку

```bash
cd argus

# Убедись что на main
git checkout main

# Скачай последние изменения
git pull

# Создай новую ветку для фичи
git checkout -b feature-cost-tracking
```

**Что произошло:** Ты создал копию кода. Теперь можешь менять что угодно - `main` не тронут.

### Шаг 2: Пиши код

```bash
# Пишешь код в VS Code
# Сохраняешь файлы

# Проверяешь что изменилось
git status

# Добавляешь изменения
git add .

# Коммитишь
git commit -m "Add cost calculation for OpenAI"
```

### Шаг 3: Загрузи ветку на GitHub

```bash
# Первый раз
git push -u origin feature-cost-tracking

# Потом просто
git push
```

**Важно:** Это загружает ветку на GitHub, но `main` не трогает!

### Шаг 4: Проверь на GitHub

Зайди на https://github.com/sh1esty1769/argus

Увидишь:
- `main` - старый код (продакшн)
- `feature-cost-tracking` - твоя новая ветка

**Никто не видит твою ветку в продакшне!**

### Шаг 5: Когда готов - слей в main

#### Вариант A: Через GitHub (рекомендую)

1. Зайди на GitHub
2. Увидишь "Compare & pull request"
3. Нажми → создастся Pull Request
4. Проверь изменения
5. Нажми "Merge pull request"
6. Нажми "Confirm merge"
7. Готово! Код в `main`

#### Вариант B: Через терминал

```bash
# Переключись на main
git checkout main

# Слей ветку
git merge feature-cost-tracking

# Загрузи на GitHub
git push

# Удали ветку (опционально)
git branch -d feature-cost-tracking
```

---

## 🔄 Ежедневный workflow

### Утро:

```bash
# Переключись на main
git checkout main

# Скачай изменения
git pull

# Создай ветку для сегодняшней работы
git checkout -b feature-today-work
```

### День:

```bash
# Пишешь код
# Коммитишь часто
git add .
git commit -m "Add feature X"
git push
```

### Вечер:

```bash
# Если готово - сливаешь в main через Pull Request
# Если не готово - оставляешь в ветке
```

---

## 🌳 Типы веток

### 1. `main` (или `master`)
- **Что:** Продакшн, стабильная версия
- **Кто видит:** Все
- **Когда менять:** Только когда фича готова

### 2. `develop`
- **Что:** Основная ветка разработки
- **Кто видит:** Ты (если запушишь)
- **Когда менять:** Постоянно

### 3. `feature-X`
- **Что:** Новая фича
- **Кто видит:** Ты
- **Когда менять:** Пока работаешь над фичей

### 4. `bugfix-X`
- **Что:** Исправление бага
- **Кто видит:** Ты
- **Когда менять:** Пока фиксишь баг

### 5. `hotfix-X`
- **Что:** Срочный фикс в продакшне
- **Кто видит:** Ты
- **Когда менять:** Когда что-то сломалось в main

---

## 📊 Примеры

### Пример 1: Добавить новую фичу

```bash
# 1. Создать ветку
git checkout -b feature-email-alerts

# 2. Написать код
# ... пишешь код ...

# 3. Коммитить
git add .
git commit -m "Add email alerts"

# 4. Загрузить
git push -u origin feature-email-alerts

# 5. Создать Pull Request на GitHub

# 6. Слить в main когда готово
```

### Пример 2: Исправить баг

```bash
# 1. Создать ветку
git checkout -b bugfix-dashboard-crash

# 2. Исправить баг
# ... фиксишь ...

# 3. Коммитить
git add .
git commit -m "Fix dashboard crash on empty data"

# 4. Загрузить и слить
git push -u origin bugfix-dashboard-crash
# Pull Request → Merge
```

### Пример 3: Экспериментировать

```bash
# 1. Создать ветку
git checkout -b experiment-new-ui

# 2. Экспериментировать
# ... пробуешь разное ...

# 3. Если не понравилось - удалить ветку
git checkout main
git branch -D experiment-new-ui

# 4. Если понравилось - слить в main
```

---

## 🎯 Правила профессионалов

### 1. Никогда не коммить в `main` напрямую
```bash
# ❌ Плохо
git checkout main
git add .
git commit -m "changes"
git push

# ✅ Хорошо
git checkout -b feature-name
git add .
git commit -m "Add feature"
git push -u origin feature-name
# Pull Request → Merge
```

### 2. Одна ветка = одна фича
```bash
# ✅ Хорошо
feature-cost-tracking
feature-email-alerts
bugfix-dashboard-crash

# ❌ Плохо
feature-everything
my-changes
test
```

### 3. Понятные названия веток
```bash
# ✅ Хорошо
feature-openai-integration
bugfix-memory-leak
hotfix-security-vulnerability

# ❌ Плохо
branch1
test
asdf
```

### 4. Часто коммитить в ветке
```bash
# Каждые 30 минут работы
git add .
git commit -m "Progress on feature X"
git push
```

### 5. Держать ветки короткими
- Работаешь над фичей 1-3 дня
- Сливаешь в main
- Удаляешь ветку
- Начинаешь новую

---

## 🔍 Полезные команды

### Посмотреть все ветки
```bash
git branch
# * feature-cost-tracking (текущая)
#   main
```

### Посмотреть ветки на GitHub
```bash
git branch -r
# origin/main
# origin/feature-cost-tracking
```

### Переключиться между ветками
```bash
git checkout main
git checkout feature-cost-tracking
```

### Удалить ветку
```bash
# Локально
git branch -d feature-name

# На GitHub
git push origin --delete feature-name
```

### Посмотреть изменения между ветками
```bash
git diff main feature-cost-tracking
```

---

## 🚨 Что делать если...

### Случайно закоммитил в main?

```bash
# 1. Откатить коммит
git reset --soft HEAD~1

# 2. Создать ветку
git checkout -b feature-name

# 3. Закоммитить там
git add .
git commit -m "Add feature"
git push -u origin feature-name
```

### Хочу перенести изменения в другую ветку?

```bash
# 1. Сохранить изменения
git stash

# 2. Переключиться на другую ветку
git checkout other-branch

# 3. Применить изменения
git stash pop
```

### Забыл на какой ветке?

```bash
git branch
# Покажет * на текущей ветке
```

---

## 📚 Стратегии

### Простая (для тебя сейчас)

```
main (продакшн)
  ↓
feature-X (новая фича)
```

Работаешь в `feature-X`, когда готово - сливаешь в `main`.

### Средняя (когда будет команда)

```
main (продакшн)
  ↓
develop (разработка)
  ↓
feature-X (фичи)
```

Все фичи сливаются в `develop`, потом `develop` → `main`.

### Продвинутая (для больших проектов)

```
main (продакшн)
  ↓
develop (разработка)
  ↓
feature-X (фичи)
hotfix-Y (срочные фиксы)
release-Z (релизы)
```

---

## ✅ Чеклист "Я умею работать с ветками"

- [ ] Могу создать ветку
- [ ] Могу переключаться между ветками
- [ ] Могу коммитить в ветку
- [ ] Могу загрузить ветку на GitHub
- [ ] Могу создать Pull Request
- [ ] Могу слить ветку в main
- [ ] Могу удалить ветку
- [ ] Понимаю зачем это нужно

---

## 🎯 Твой план на сегодня

1. Создай ветку `develop`:
   ```bash
   git checkout -b develop
   git push -u origin develop
   ```

2. Теперь всегда работай в ветках:
   ```bash
   git checkout develop
   git checkout -b feature-new-thing
   # пишешь код
   git push -u origin feature-new-thing
   ```

3. Когда готово - Pull Request → `main`

---

**Теперь ты работаешь как профессионал! 🚀**
