# Git - Только то что тебе нужно

## 🎯 5 команд на каждый день

```bash
# 1. Посмотреть что изменилось
git status

# 2. Добавить все изменения
git add .

# 3. Сохранить с описанием
git commit -m "описание что сделал"

# 4. Загрузить на GitHub
git push

# 5. Скачать с GitHub
git pull
```

**Это 90% того что ты будешь использовать!**

---

## 📝 Пример реальной работы

```bash
# Утро - начинаешь работу
cd agentwatch
git pull  # скачать последние изменения

# Открываешь VS Code, пишешь код
# Сохраняешь файлы (Cmd+S)

# Посмотреть что изменилось
git status
# Покажет: modified: agentwatch/watch.py

# Добавить изменения
git add .

# Сохранить с описанием
git commit -m "Add OpenAI cost calculation"

# Загрузить на GitHub
git push

# Готово! Код на GitHub
```

---

## 🔄 Работа с ветками (для новых фич)

```bash
# Создать ветку для новой фичи
git checkout -b feature-alerts

# Написать код, сохранить

# Коммит
git add .
git commit -m "Add email alerts"

# Загрузить ветку на GitHub
git push -u origin feature-alerts

# Зайти на GitHub → Create Pull Request
# После одобрения - слить в main

# Вернуться на main
git checkout main
git pull
```

---

## ⚠️ Если что-то пошло не так

### Случайно изменил файл
```bash
git checkout -- filename.py
```

### Хочу отменить последний коммит
```bash
git reset --soft HEAD~1
# Изменения останутся, коммит удалится
```

### Конфликт при git pull
```bash
# 1. Открой файл с конфликтом
# 2. Найди строки с <<<<<<< и >>>>>>>
# 3. Выбери нужную версию
# 4. Удали маркеры
git add .
git commit -m "Resolve conflict"
```

---

## 📊 Посмотреть историю

```bash
# Полная история
git log

# Короткая история
git log --oneline

# Последние 5 коммитов
git log -5 --oneline
```

---

## 🎓 Практика прямо сейчас

```bash
# 1. Создай тестовый файл
echo "test" > test.txt

# 2. Посмотри статус
git status

# 3. Добавь
git add test.txt

# 4. Коммит
git commit -m "Add test file"

# 5. Загрузи
git push

# 6. Зайди на GitHub - увидишь файл!

# 7. Удали тестовый файл
rm test.txt
git add test.txt
git commit -m "Remove test file"
git push
```

---

## 💡 Советы

1. **Коммить часто** - каждая логическая часть работы
2. **Понятные сообщения** - через месяц забудешь что делал
3. **git status перед коммитом** - проверь что добавляешь
4. **git pull перед работой** - избежишь конфликтов
5. **Не бойся ошибок** - всегда можно откатить

---

## 🚀 Твой workflow

### Каждый день:
```bash
git pull          # скачать изменения
# пишешь код
git add .         # добавить
git commit -m ""  # сохранить
git push          # загрузить
```

### Новая фича:
```bash
git checkout -b feature-name  # новая ветка
# пишешь код
git add .
git commit -m ""
git push -u origin feature-name
# Pull Request на GitHub
```

### Исправление бага:
```bash
git checkout -b bugfix-name
# фиксишь баг
git add .
git commit -m "fix: описание бага"
git push -u origin bugfix-name
# Pull Request на GitHub
```

---

## 📚 Больше информации

- `GIT_GUIDE.md` - полное руководство
- `GIT_CHEATSHEET.md` - все команды
- https://learngitbranching.js.org/ - интерактивное обучение

---

## ✅ Чеклист "Я знаю Git"

- [ ] Могу создать коммит
- [ ] Могу загрузить на GitHub
- [ ] Могу скачать с GitHub
- [ ] Могу создать ветку
- [ ] Могу посмотреть историю
- [ ] Могу отменить изменения
- [ ] Понимаю что такое merge
- [ ] Могу решить конфликт

Если все галочки - ты знаешь Git! 🎉

---

**Помни**: Git - это как велосипед. Сначала страшно, потом не замечаешь.

Практикуй каждый день и через неделю будет автоматом!

Удачи! 🚀
