# Git Guide - Полное руководство для начинающих

## 📚 Что такое Git?

Git - это система контроля версий. Она позволяет:
- 💾 Сохранять историю изменений кода
- ⏮️ Откатываться к предыдущим версиям
- 👥 Работать в команде над одним проектом
- ☁️ Хранить код в облаке (GitHub)

## 🎯 Основные концепции

### Repository (Репозиторий)
Папка с твоим проектом + история всех изменений

### Commit (Коммит)
"Снимок" состояния кода в определенный момент времени

### Branch (Ветка)
Параллельная версия кода (для экспериментов)

### Remote (Удаленный репозиторий)
Копия репозитория на GitHub

---

## 🚀 Основные команды Git

### 1. Инициализация репозитория

```bash
# Создать новый Git репозиторий в текущей папке
git init

# Проверить статус (какие файлы изменены)
git status
```

**Что происходит**: Git создает скрытую папку `.git` где хранит всю историю.

---

### 2. Добавление файлов

```bash
# Добавить конкретный файл
git add README.md

# Добавить все файлы
git add .

# Добавить все Python файлы
git add *.py

# Посмотреть что добавлено
git status
```

**Staging Area** - это "зона ожидания". Файлы сначала добавляются сюда, потом коммитятся.

---

### 3. Создание коммита

```bash
# Создать коммит с сообщением
git commit -m "Initial commit"

# Посмотреть историю коммитов
git log

# Короткая история
git log --oneline
```

**Хорошие сообщения коммитов**:
- ✅ "Add user authentication"
- ✅ "Fix bug in payment processing"
- ✅ "Update README with installation guide"
- ❌ "fix"
- ❌ "changes"
- ❌ "asdfasdf"

---

### 4. Подключение к GitHub

```bash
# Добавить удаленный репозиторий
git remote add origin https://github.com/sh1esty1769/AI-for-observability.git

# Проверить удаленные репозитории
git remote -v

# Переименовать ветку в main (стандарт GitHub)
git branch -M main
```

---

### 5. Загрузка кода на GitHub

```bash
# Первая загрузка
git push -u origin main

# Последующие загрузки
git push
```

**Что происходит**: Твой код загружается на GitHub и становится доступен всему миру.

---

### 6. Скачивание изменений

```bash
# Скачать изменения с GitHub
git pull

# Скачать без слияния
git fetch
```

---

### 7. Ветки (Branches)

```bash
# Создать новую ветку
git branch feature-new-dashboard

# Переключиться на ветку
git checkout feature-new-dashboard

# Создать и сразу переключиться
git checkout -b feature-new-dashboard

# Посмотреть все ветки
git branch

# Удалить ветку
git branch -d feature-new-dashboard
```

**Зачем нужны ветки?**
- `main` - стабильная версия (production)
- `develop` - разработка
- `feature-X` - новая фича
- `bugfix-Y` - исправление бага

---

### 8. Слияние веток (Merge)

```bash
# Переключиться на main
git checkout main

# Слить feature ветку в main
git merge feature-new-dashboard
```

---

### 9. Отмена изменений

```bash
# Отменить изменения в файле (до add)
git checkout -- README.md

# Убрать файл из staging (после add)
git reset HEAD README.md

# Откатить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Откатить последний коммит (удалить изменения)
git reset --hard HEAD~1
```

⚠️ **Осторожно с `--hard`** - это удаляет изменения навсегда!

---

### 10. Просмотр изменений

```bash
# Посмотреть что изменилось (до add)
git diff

# Посмотреть что в staging
git diff --staged

# Посмотреть историю изменений файла
git log -p README.md
```

---

## 🔄 Типичный рабочий процесс

### Ежедневная работа:

```bash
# 1. Скачать последние изменения
git pull

# 2. Создать ветку для новой фичи
git checkout -b feature-cost-tracking

# 3. Написать код, сохранить файлы

# 4. Посмотреть что изменилось
git status
git diff

# 5. Добавить изменения
git add .

# 6. Создать коммит
git commit -m "Add cost tracking for OpenAI"

# 7. Загрузить на GitHub
git push -u origin feature-cost-tracking

# 8. Создать Pull Request на GitHub
# (через веб-интерфейс)

# 9. После одобрения - слить в main
git checkout main
git merge feature-cost-tracking
git push
```

---

## 🎯 Для твоего проекта AgentWatch

### Первая загрузка:

```bash
cd agentwatch

# 1. Инициализировать Git
git init

# 2. Добавить все файлы
git add .

# 3. Первый коммит
git commit -m "Initial commit - AgentWatch v0.1.0"

# 4. Подключить GitHub
git remote add origin https://github.com/sh1esty1769/AI-for-observability.git

# 5. Переименовать ветку
git branch -M main

# 6. Загрузить
git push -u origin main
```

### Последующие обновления:

```bash
# 1. Изменить файлы

# 2. Посмотреть что изменилось
git status

# 3. Добавить изменения
git add .

# 4. Коммит
git commit -m "Add OpenAI cost calculation"

# 5. Загрузить
git push
```

---

## 🔧 Полезные команды

```bash
# Посмотреть конфигурацию
git config --list

# Изменить последний коммит
git commit --amend -m "New message"

# Посмотреть кто что менял
git blame README.md

# Найти коммит где появился баг
git bisect start

# Сохранить изменения временно
git stash
git stash pop

# Клонировать чужой репозиторий
git clone https://github.com/user/repo.git
```

---

## 📝 .gitignore - Что не загружать

Создай файл `.gitignore` чтобы не загружать мусор:

```
# Python
__pycache__/
*.pyc
*.pyo
*.db
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Secrets
.env
*.key
```

---

## 🚨 Частые ошибки и решения

### Ошибка: "fatal: not a git repository"
**Решение**: Ты не в папке с Git. Сделай `git init`

### Ошибка: "Your branch is behind"
**Решение**: `git pull` чтобы скачать изменения

### Ошибка: "merge conflict"
**Решение**: 
1. Открой файл с конфликтом
2. Найди строки с `<<<<<<<`, `=======`, `>>>>>>>`
3. Выбери нужную версию
4. Удали маркеры
5. `git add .` и `git commit`

### Ошибка: "Permission denied (publickey)"
**Решение**: Настрой SSH ключ или используй HTTPS

---

## 🎓 Продвинутые темы

### SSH ключи (для удобства)

```bash
# Создать SSH ключ
ssh-keygen -t ed25519 -C "your-email@example.com"

# Скопировать ключ
cat ~/.ssh/id_ed25519.pub

# Добавить на GitHub:
# Settings → SSH Keys → New SSH Key
```

### Git Flow (для больших проектов)

```
main (production)
  ↓
develop (разработка)
  ↓
feature/X (новые фичи)
hotfix/Y (срочные фиксы)
```

---

## 📚 Ресурсы для изучения

### Интерактивные туториалы:
- https://learngitbranching.js.org/ (визуальное обучение)
- https://git-scm.com/book/ru/v2 (официальная книга на русском)

### Шпаргалки:
- https://education.github.com/git-cheat-sheet-education.pdf

### YouTube:
- "Git и GitHub для начинающих" (на русском)

---

## 🎯 Практика

Попробуй сам:

1. Создай тестовую папку
2. `git init`
3. Создай файл `test.txt`
4. `git add test.txt`
5. `git commit -m "Add test file"`
6. Измени файл
7. `git diff` - посмотри изменения
8. `git add .` и `git commit -m "Update test"`
9. `git log` - посмотри историю

---

## 💡 Советы

1. **Коммить часто** - лучше 10 маленьких коммитов чем 1 большой
2. **Писать понятные сообщения** - через месяц ты забудешь что делал
3. **Использовать ветки** - не ломай main
4. **Делать pull перед push** - избежишь конфликтов
5. **Не коммитить секреты** - API ключи, пароли в .gitignore

---

## 🚀 Готов загружать AgentWatch?

Следуй инструкциям выше и твой код будет на GitHub!

Удачи! 🎉
