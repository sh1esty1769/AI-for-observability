# Git Шпаргалка - Быстрый справочник

## 🚀 Основные команды (используй каждый день)

```bash
# Посмотреть статус (что изменилось)
git status

# Добавить все изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push

# Скачать с GitHub
git pull
```

---

## 📝 Типичный рабочий процесс

```bash
# 1. Изменил файлы в редакторе
# 2. Сохранил (Cmd+S)

# 3. Посмотреть что изменилось
git status
git diff

# 4. Добавить изменения
git add .

# 5. Коммит с описанием
git commit -m "Add new feature"

# 6. Загрузить на GitHub
git push
```

---

## 🔄 Работа с ветками

```bash
# Создать новую ветку
git checkout -b feature-name

# Переключиться на другую ветку
git checkout main

# Посмотреть все ветки
git branch

# Слить ветку в текущую
git merge feature-name

# Удалить ветку
git branch -d feature-name
```

---

## ⏮️ Отмена изменений

```bash
# Отменить изменения в файле (до git add)
git checkout -- filename.py

# Убрать из staging (после git add)
git reset HEAD filename.py

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалить изменения) ⚠️
git reset --hard HEAD~1
```

---

## 📊 Просмотр истории

```bash
# История коммитов
git log

# Короткая история
git log --oneline

# Что изменилось
git diff

# Кто что менял
git blame filename.py
```

---

## 🌐 Работа с GitHub

```bash
# Первая загрузка
git push -u origin main

# Обычная загрузка
git push

# Скачать изменения
git pull

# Клонировать чужой репозиторий
git clone https://github.com/user/repo.git
```

---

## 🔧 Настройка

```bash
# Настроить имя
git config --global user.name "Your Name"

# Настроить email
git config --global user.email "your@email.com"

# Посмотреть настройки
git config --list
```

---

## 🆘 Частые проблемы

### "Your branch is behind"
```bash
git pull
```

### "merge conflict"
```bash
# 1. Открой файл с конфликтом
# 2. Найди <<<<<<< и >>>>>>>
# 3. Выбери нужную версию
# 4. Удали маркеры
git add .
git commit -m "Resolve conflict"
```

### Забыл что-то добавить в последний коммит
```bash
git add forgotten-file.py
git commit --amend --no-edit
```

### Случайно закоммитил секреты
```bash
# Удалить файл из Git (но оставить локально)
git rm --cached .env
echo ".env" >> .gitignore
git commit -m "Remove .env from Git"
git push
```

---

## 💡 Полезные алиасы

Добавь в `~/.gitconfig`:

```
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    unstage = reset HEAD --
    last = log -1 HEAD
    visual = log --oneline --graph --all
```

Теперь можно:
```bash
git st      # вместо git status
git co main # вместо git checkout main
git visual  # красивая история
```

---

## 📚 Для твоего проекта

### Первый раз:
```bash
cd agentwatch
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/sh1esty1769/AI-for-observability.git
git push -u origin main
```

### Каждый день:
```bash
# Написал код
git add .
git commit -m "Add cost tracking"
git push
```

### Новая фича:
```bash
git checkout -b feature-alerts
# Написал код
git add .
git commit -m "Add email alerts"
git push -u origin feature-alerts
# Создай Pull Request на GitHub
```

---

## 🎯 Правила хороших коммитов

### ✅ Хорошо:
- "Add user authentication"
- "Fix bug in payment processing"
- "Update README with examples"
- "Refactor database queries"

### ❌ Плохо:
- "fix"
- "changes"
- "asdfasdf"
- "work in progress"

### Формат:
```
<тип>: <краткое описание>

<подробное описание (опционально)>
```

Типы:
- `feat:` - новая фича
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - рутина

Пример:
```bash
git commit -m "feat: add OpenAI cost calculation

- Calculate cost based on tokens
- Support GPT-3.5 and GPT-4
- Add tests for cost calculation"
```

---

## 🚨 Что НЕ коммитить

Добавь в `.gitignore`:

```
# Секреты
.env
*.key
secrets.json

# Python
__pycache__/
*.pyc
venv/

# Базы данных
*.db
*.sqlite

# IDE
.vscode/
.idea/

# OS
.DS_Store
```

---

## 📱 GitHub Desktop (альтернатива)

Если командная строка пугает, используй GitHub Desktop:
- https://desktop.github.com/
- Визуальный интерфейс
- Все то же самое, но с кнопками

---

## 🎓 Практика

Попробуй прямо сейчас:

```bash
# 1. Создай тестовый файл
echo "Hello Git" > test.txt

# 2. Посмотри статус
git status

# 3. Добавь файл
git add test.txt

# 4. Коммит
git commit -m "Add test file"

# 5. Загрузи на GitHub
git push

# 6. Зайди на GitHub и увидишь файл!
```

---

## 🔗 Полезные ссылки

- Твой репозиторий: https://github.com/sh1esty1769/AI-for-observability
- Интерактивное обучение: https://learngitbranching.js.org/
- Официальная документация: https://git-scm.com/doc

---

## 💪 Следующий уровень

Когда освоишь базу, изучи:
- Git rebase (переписывание истории)
- Git cherry-pick (выбор конкретных коммитов)
- Git stash (временное сохранение)
- Git hooks (автоматизация)
- Git submodules (подпроекты)

---

**Помни**: Git - это навык на всю жизнь. Все программисты его используют. Чем раньше освоишь, тем лучше!

Удачи! 🚀
